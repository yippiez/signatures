const ROOT_KEY = …
class Outer
  constructor(value)
  getInner()
    class Inner
      constructor(x)
      compute()
  makeCounter()
    function increment()
    function reset()
  static create(value)
function pipeline(...fns)
const memoize = (fn) =>
    const key = …
    const result = …
function makeAdder(x)
class EventBus
  constructor()
  on(event, handler)
  off(event, handler)
  emit(event, ...args)
