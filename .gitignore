# Compiled output / build artifacts
/target/

# Backtrace / debug artifacts
*.pdb

# Cargo lock for libraries (this is a binary crate, so keep it — left commented
# as a reminder of the convention)
# Cargo.lock

# Editor / OS noise
*.swp
*~
.DS_Store
